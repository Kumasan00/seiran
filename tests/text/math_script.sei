\section{スクリプトの範囲}

上付き・下付きの内容は波括弧が閉じるので、直後の区切り文字の種類でスコープが変わらない（#486）。
$x^{2}+y$ と $x^{2}=y$ と $x^{2},y$ と $x_{i}(j)$ と $x^{a}b$ は
いずれもスクリプトが 1 個のグループで閉じ、残りは核の外側に並ぶ。

\begin{equation}
a_{i}^{2}+b_{i}^{2}=c_{i}^{2}
\end{equation}
